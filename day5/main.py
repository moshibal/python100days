def format_name(f_name,l_name):
    """take two inputs and convert them to upper cases"""
    name=f_name.upper()
    last_name=l_name.upper()
    return (name+" "+last_name)
print(format_name("bishal","karki")) 